SQL Executor App

This project is a simple SQL Executor App using ReactJS for the frontend and NodeJS for the backend, connecting to a MySQL database. It allows users to execute custom SQL queries from the frontend interface and displays the results.
